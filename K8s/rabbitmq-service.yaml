apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: simple-bank
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: http
      port: 80
      targetPort: 15672
